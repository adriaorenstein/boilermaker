import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <div>Hello, world!</div>,
  document.getElementById("app")
  //make sure 'app' is the same as the id of the div in index.html
);
